# プロジェクト引き継ぎ書 — COSMIC ID

監査日: 2026-07-16(Fable)/ 監査時点の HEAD: `c88e640`(main)
凡例: **[事実]** = このセッションで実測・実行して確認したもの / **[推測]** = 状況からの推定(未検証)

急いでいる人は **§10(5分で読める引き継ぎ書)** だけ読めばよい。

---

## 1. プロジェクトの目的(1文)

生年月日と名前の入力だけで、19の占術・スピリチュアル体系を横断解釈し一つの「物語」として提示する、基本無料+Premium課金予定のWebアプリ(正確さより物語性を優先しつつ、計算の誠実さを信頼の土台にする)。

## 2. 完成済みのもの

**[事実]**(すべて main にマージ済み・`npm test` 98件passで確認):
- **計算の正確性改修一式**: マヤ暦KIN(公式ドリームスペル準拠: 起点KIN164・2/29スキップ・関連KIN式修正)/lifePath(個別還元方式)/タロット22=愚者マップ/干支・五行・九星の年切替を立春系に統一(1900–2100の天文計算立春テーブル `src/data/risshun.js`)/月星座の本実装(astronomy-engine・境界日は両候補表示)/cyclesDayPlan月相バグ修正
- **注記一式**: カスプ注記・「独自の簡易指標」明示(月命星・日命星等)・動物占い独自12分類明記・ケルトja和名修正(ja/en同期済み)
- **UI/PWA**: 375pxタップ領域+デザインポリッシュ(Cursor作)/PWA化(正方形アイコン・Service Worker・実オフラインで診断まで動作確認済み)
- **回帰テスト**: `src/__tests__/` 98件(期待値は[既知値]/[外部照合]/[回帰ピン]で出どころ分類)
- **調査資産**: `docs/audit/`(流派ズレ監査・出典付き)、`docs/learnings/`(教訓6件)
- **運用基盤**: GitHub Issues #1–16・ラベル(agent:*/phase:*)、`docs/ORG.md`(体制+コスト効率ルールv2)、`.cursor/rules/`(Cursor用規約)、Issue #1–15 クローズ済み

## 3. 未完成のもの

- **[事実] GitHub Pagesデプロイが失敗し続けている**(§6参照)— **本番サイトに最近の修正が反映されていない**
- **[事実]** Issue #16(Stripe決済の設計)— 唯一のオープンIssue。価格・解放範囲の決定待ち
- **[事実]** Premium課金フロー自体が未実装(CTAは `alert()` のまま。さらに現在は全章無料に一時開放中 — コミット `cb6a5bd`)
- **[事実]** アカウント・データ永続化・相性保存等のロードマップ項目(README記載)は未着手

## 4. 重要な意思決定(すべて確定済み・蒸し返さない)

**[事実]**(GitHub Issue #1 のコメントに記録、依頼主 Koyo が決定):
1. マヤ暦KIN: 照合の結果「黒」だったため**公式ドリームスペル準拠に修正**(実施済み)
2. タロット還元22: **愚者(0)にマップ**(Mary K. Greer方式)
3. lifePath: **個別還元方式**(Numerology.com/Decoz系)に変更
4. 年の切替: 干支・五行を九星と同じ**立春系に統一**+UI注記
5. 動物占い: **「独自12分類」と明記**(公式互換化は見送り。PR #23 の照合で公式と3層非互換を確認済み)
- 立春の定義は**天文計算**(太陽黄経315°・JST。特定の暦注ではない)を採用(PR #20)
- プロトタイプ段階のため計算結果変動の**ユーザー告知は不要**(Issue #1 注記)
- 開発体制は `docs/ORG.md` v2: 担当はCursor→Sonnet→Opus→Fableのコスト階層順、Fableは設計とレビューのみ

## 5. 保留中の判断(勝手に決めてはいけない)

- **[事実]** #16: Premiumの**価格・解放範囲**(人間の経営判断待ち)
- **[事実]** Premiumゲートをいつ再有効化するか(現在は意図的に全章無料公開中)
- **[事実]** マヤ暦ガイドKINの紋章オフセット表の**一次資料照合**が未完(実装は定説+既知オラクル一致で妥当性確認済みだが、13moon.com等での逐条照合は残)
- **[事実]** 月星座の外部サイト照合が未実施(テストは astronomy-engine 出力の回帰ピン。§6参照)

## 6. 既知のバグ・リスク

- **【最重要・要即対応】[事実] Deploy to GitHub Pages が失敗し続けている**。
  原因は CI の `npm ci` が **package-lock.json と package.json の不整合**で落ちるため(`Missing: esbuild@0.28.1 from lock file` 等)。直近8回(2026-07-14 12:18 以降=PR #23 マージ以降)すべて failure を確認。
  **[推測]** vitest 追加時に lockfile が正しく再生成されなかったのが原因。**[推測]** 本番サイトは PR #22 以前の状態のまま(どのコミットまで反映済みかは未確認 — 本番URLで月星座カードの有無を見れば判別可能)。
  → 修正手順は §7-1。
- **[事実]** 月星座の回帰ピン(1990-05-15→山羊座 等)は外部照合未実施。ズレが発覚した場合は `eclipticLongitudeToSignIndex` の +3 オフセットを疑う(テストコメントに記載)
- **[事実]** 木星回帰タイムライン(SVG)の目盛りマーカーが幅17pxで細い(PR #27 レビューで検出・実タップ対象かは cursor:pointer のみで onclick なし。実害は小さい **[推測]**)
- **[事実]** 2/29生まれのマヤ暦KINは「2/28と同じ」を返す暫定仕様(公式は「KINなし」。docs/learnings/maya-feb29-no-kin.md)
- **[推測]** 並行して別のClaudeセッションやCursorが同リポジトリを触ることがある(過去に実績)。作業前の `git fetch` と、ブランチ名 `issue/` プレフィクスの遵守で衝突を避けること

## 7. 次にやるべき作業(優先順)

1. **CIデプロイ修復**: ローカルで `npm install`(lockfile再同期)→ `npm test` と `npm run build` がpassすることを確認 → package-lock.json をコミット・push → Actions が green になることを確認【Sonnet可・30分】
2. **本番スモークテスト**: デプロイ成功後、https://tannyann.github.io/cosmic-id/ で 1990-05-15 を入力し、月星座カード表示・undefined混入なし・PWAインストール可を確認【Sonnet可】
3. **#16 の価格決定**(人間)→ 決定後に Stripe 実装設計を Opus に下書きさせる(AGENTS.md の「アンロックはサーバー検証」原則厳守)
4. **月星座の外部照合**: 無料の月星座計算サイトで2〜3件照合し、テストの[回帰ピン]を[外部照合]に昇格 or 修正【Opus・調査】
5. **マヤ暦ガイドKINの一次照合**: 13moon.com / lawoftime.org のオラクル図で逐条確認【Opus・調査】
6. **Premiumゲート再有効化の判断**(人間)+ 実装(#16 とセットが自然)
7. **docs/ISSUES.md の状態列を全✅に同期**(GitHub側が正・表が古い)【Sonnet・10分】
8. **README の「計算の正確性について」節を改修後の実態に更新**(現状は旧実装の弱点告白のまま)【Sonnet】
9. **木星回帰SVGマーカーのタップ領域改善**(必要なら別Issue化)【Cursor】
10. **Codexスキル候補(docs/Codexスキル候補一覧.md)を実スキル/コマンド化**して次フェーズの委任を型化【人間+Sonnet】

## 8. 実行してはいけない変更

- `~/Documents/Claude/cosmic_id` と `~/Documents/GitHub/cosmic-id` を編集しない(**古いコピー**。正本は `/Users/mizutanikouyou/cosmic_id` のみ)
- `escapeHtml()` を通さずにユーザー入力を描画しない(`eval`/`innerHTML`直渡し禁止)
- プレミアムぼかしロックの仕組み(`isPremiumDemo` × `.premium-section.unlocked`)を壊さない。アンロック状態を localStorage に持たせない(サーバー検証が前提)
- `MAYA_SEALS`/`MAYA_TONES` など辞書配列の**並び順を変えない**(インデックスが計算と結合している)
- 「言葉は content/deeper(i18n locales)、計算は calculations、画面は ui」の分離を崩さない
- I-1 で確定した流派・方式(§4)を再変更しない(変更は依頼主の決定が必要)
- ja/en の一方だけ文言変更しない(必ず同時同キー構造)
- 数MBのバイナリ素材をリポジトリにコミットしない(.gitignore 済み)
- 断定的な予言・ネガティブ断定・医療/法律/財務助言をコンテンツに書かない
- 高価モデル(Fable)で実装・機械作業をしない(ORG.md v2)

## 9. 主要ファイルの役割

| パス | 役割 |
|---|---|
| `AGENTS.md` | **最初に読む規約の脳**(関心の分離・禁則・既知の検証値・テスト運用) |
| `CLAUDE.md` / `.cursor/rules/` | Claude用/Cursor用の即戦力メモ(内容はAGENTS.mdのダイジェスト) |
| `src/calculations.js` | 全占術の計算(純粋関数)。流派コメント付き |
| `src/data/risshun.js` | 立春日テーブル1900–2100(自動生成・手編集禁止。再生成は risshun.gen.mjs) |
| `src/i18n/locales/{ja,en}/` | 文言の正(content=辞書、ui=UI文言、deeper=深掘り)。**ルートの content.js は旧構成の名残** |
| `src/ui.js` | 描画・カード・モーダル(計算は呼ぶだけ) |
| `src/__tests__/` | 回帰テスト98件+ロケール固定スタブ(**calculations より先に import 必須**) |
| `public/sw.js` / `manifest.webmanifest` | PWA(Service Worker はscope基準URLでサブパス対応) |
| `.github/workflows/deploy-pages.yml` | main push で Pages へデプロイ(**現在 lockfile 不整合で失敗中**) |
| `docs/ORG.md` | 体制・レビュー品質ゲート・**コスト効率ルールv2**(運用の憲法) |
| `docs/ISSUES.md` | Issue設計書(状態はGitHub Issuesが正) |
| `docs/audit/` | 占術の流派差調査(出典付き)。式を触る前に必読 |
| `docs/learnings/` | 教訓1件1ファイル(ロケールスタブ・呼び出し元直し忘れ・2/29仕様 等) |
| `docs/CURSOR-BRIEFS.md` | Cursorへの委任ブリーフ(貼るだけで動く) |

## 10. 5分で読める引き継ぎ書

**何のアプリか**: 生年月日+名前 → 19占術を横断解釈する無料Web占い(Premium課金は準備中)。Vite + vanilla JS、i18n(ja/en)、GitHub Pages配信。

**どこで作業するか**: 正本は `/Users/mizutanikouyou/cosmic_id`(他2箇所のコピーは触らない)。remote は github.com/tannyann/cosmic-id。着手前に `AGENTS.md` → `docs/ORG.md` の順で読む。

**今どういう状態か**: 計算の正確性改修(マヤ暦・数秘・立春統一・月星座ほか)からPWA化まで、Issue #1–15 が完了しmainにマージ済み。テストは `npm test` で98件全pass。**ただし GitHub Pages のデプロイCIが lockfile 不整合で失敗し続けており、本番サイトには最近の修正が乗っていない(最優先で修復: `npm install` → lockfile コミット)**。残るオープンIssueは #16(Stripe設計・価格は人間の決定待ち)のみ。

**働き方のルール(要点)**: ①1 Issue=1ブランチ(`issue/I-N-slug`)=1 PR、PRに `Closes #N` ②分身に振るときは即コミット/こまめpush(途中死対策)・並列2体まで・プロンプト10行以内 ③担当はCursor(定額)→Sonnet→Opus→Fableの順で安い方から ④検証は npm test → node → ブラウザの順、実行結果を貼る、未確認は「未確認」と書く ⑤計算式を触るときは流派コメント+レビューは作者以外 ⑥流派選定・課金・デザイン方針は人間の判断。詳細は `docs/ORG.md`。

**次の一手**: §7 の 1→2(CI修復と本番確認)。その後は #16 の価格決定待ち。
