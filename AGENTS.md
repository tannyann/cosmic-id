# AGENTS.md — AI コーディング支援への指針

Cursor、Claude Code、Copilot などの AI ツールがこのコードベースを編集する時の規約集。
人間の README が「何を作っているか」なら、これは「どう変えるか」の手引き。

---

## このプロジェクトのゴール

**個人の生年月日と名前から、複数の占術・スピリチュアル体系を横断解釈する Web アプリ。**

「占いごとに別のアプリを開く必要をなくす」がコア体験。「正確さ」より「物語性」と「自己理解の楽しさ」が優先。

- ターゲットユーザー:占いやスピリチュアルに親しんでいる一般層(コアユーザーは女性が多い想定)
- 収益モデル:基本無料（十九の体系・サマリー・各カードの無料解釈・シェア）→ かなり深く知りたい人だけ Premium（深層解釈・将来は相性・10年タイムライン等）
- トーン:神秘的だが押しつけがましくない。「言い切らない」「あなたの物語はあなたが書く」

---

## アーキテクチャの原則

### 関心の分離

| ファイル | 含めて OK | 含めない |
| --- | --- | --- |
| `content.js` | 静的辞書(意味・説明文・データテーブル) | 計算、DOM、副作用 |
| `calculations.js` | 純粋関数の計算ロジック | DOM、コンテンツ文字列の直書き(辞書から引く) |
| `deeper.js` | モーダル詳細コンテンツの構築 | 計算ロジック、DOM 操作 |
| `ui.js` | DOM 描画、イベント、状態 | 計算ロジック(`calculations.js` を呼ぶ) |
| `styles.css` | 見た目すべて | — |

**「言葉は content/deeper、計算は calculations、画面は ui」を厳守。**

### 計算関数は純粋に保つ

`calculations.js` の関数は、同じ入力に対して常に同じ出力を返す。`Date.now()` や `localStorage` 等の副作用を持ち込まない。

唯一の例外は今日の日付を使う `biorhythm()`、`moonPhaseToday()`、`dailyTarot()`、`lifeStage()`。これらは「今日を入力に含む」と理解する。

### 新しい体系を追加する手順(README にも記載)

1. `content.js` に辞書(`export const`)
2. `calculations.js` に純粋関数(`export function`)
3. `ui.js` の `render()` に `card('newKey', ...)` を追加
4. `deeper.js` の `buildDeep()` switch に `case 'newKey'` を追加(free と premium の両方)
5. `README.md` の体系一覧に追記

順番を守ると後で迷わない。

---

## コードスタイル

- **インデント**:スペース 2(全ファイル)
- **クォート**:JavaScript はシングル `'` 、HTML 属性内はダブル `"`
- **命名**
  - 静的辞書:`SCREAMING_SNAKE_CASE`(`LIFE_PATH_MEANINGS`)
  - 関数:`camelCase`(`lifePath`、`sunSign`)
  - DOM ID:`kebab-case` または短い英単語(`modal`、`demo-premium`)
  - CSS クラス:`kebab-case`
- **コメント**:なぜ(why)を書く。何を(what)はコードで読める
- **ファイル冒頭**:JSDoc コメントで責務を説明

---

## やってはいけないこと

### ❌ 占術の結果を断定的に書かない

```
✗ 「あなたは必ず35歳で結婚します」
○ 「35歳前後に出会いの波が訪れることが多いとされます」
```

スピリチュアル系コンテンツは、ユーザーが自分で意味を見出すための「触媒」として書く。

### ❌ メンタルヘルス的に危険な文言を入れない

「絶望」「終わり」「孤独で死ぬ」のような断定的にネガティブな表現は避ける。死神カード等の伝統的に重い象徴も「変容」「再生」の方向で記述する。

### ❌ 占い結果に基づいて医療・法律・財務の助言を出さない

CTA や深掘りコンテンツで「ライフパス8だから株を買おう」のような誘導はしない。

### ❌ プレミアムオーバーレイの DOM を弄らずにロックを外さない

`isPremiumDemo` フラグと CSS の `.premium-section.unlocked` の関係を維持する。決済成功時のアンロックは、別途状態管理を追加して行う(`localStorage` ではなく、サーバー側で検証された JWT 等)。

### ❌ `eval` / `Function()` / `innerHTML` にユーザー入力を直接渡さない

現在は `escapeHtml()` を通している。新しいユーザー入力を扱う時も必ず通すこと。

---

## 推奨される変更パターン

### 文言を変えたい

→ `content.js` を編集。コードは触らない。

### 占術の計算式を直したい

→ `calculations.js` を編集。`content.js` の辞書のキー(`MAYA_SEALS` のインデックス等)は変えない。

### 新しい UI を加えたい(例:結果のシェアボタン)

→ `index.html` に要素 → `styles.css` にスタイル → `ui.js` にロジック。

### 課金フローを実装したい

→ `ui.js` の `renderModalBody()` 内、`onclick="alert(...)"` を実際の決済フローに差し替え。
→ アンロック状態は localStorage ではなく、ユーザーアカウント+サーバー検証で持つこと。

### AI 生成のパーソナル解釈を入れたい

→ `deeper.js` の `buildDeep()` 内の premium 配列を、非同期で LLM API から取得するように変更。
→ モーダル展開時は「読み込み中…」プレースホルダを出し、ストリーミングで埋める。
→ API キーはフロントエンドに置かない。バックエンド経由で呼ぶ。

---

## テストとデバッグ

ユニットテストは `npm test`(Vitest)。`src/__tests__/calculations.test.js` が
計算ロジックの回帰テスト。テストは日本語ロケール前提のため、`setup-locale.js`
(localStorage スタブ)を **calculations.js より先に import** する規約
(理由は `docs/learnings/vitest-locale-stub.md`)。「今日」依存の関数は
`vi.setSystemTime` で固定する。期待値には出どころを明記する:
[既知値](本ファイル由来)/[外部照合](一般事実)/[回帰ピン](現状実装の固定)。

追加の検証済み既知値(2026-07 監査時に導出):

- 1990-05-15 → 九星 一白水星、六十干支 庚午(金・陽)、タロット 3 女帝
- 1984 → 甲子(六十干支の起点)、2024 → 甲辰
- マヤ暦(ドリームスペル公式)は 2013-07-26 = KIN 164「銀河の黄色い種」/ 1987-07-26 = KIN 34。2/29 は KIN を持たない日としてスキップする(Issue #7 で修正済み。`docs/audit/verify/maya-verification.md` 参照)

既知バグ(方針判断待ちのため未修正。現状挙動をテストで固定済み):

- `tarotBirthCard`: 合計がちょうど 22 のとき name が undefined(`docs/learnings/tarot-birthcard-22-undefined.md`)
- `cyclesDayPlan`: moonPhase / moonName が常に undefined(`docs/learnings/cyclesdayplan-moonphase-undefined.md`)

占術計算の流派差・正確性の棚卸しは `docs/audit/fortune-accuracy.md`(全体)と
`docs/audit/parts/others.md`(出典付き詳細)。残作業は `docs/HANDOFF.md`。

手動チェックリスト:

1. **計算検証**:既知の値で確認
   - 1990-05-15 → ライフパス 3、午年
   - 2024 年 → 九星気学 三碧木星、辰年
   - 2000-01-01 → ライフパス 4、九星は立春補正で一白水星(暦年のみなら九紫火星)
2. **境界値**
   - 立春前(2月1日)生まれの九星気学が前年扱いになるか
   - 12月31日 → 山羊座、24日 → 山羊座(冬至期間の境界)
3. **モーダル開閉**:全カードがクリック/キーボードでモーダルを開けるか
4. **深層解釈デモ切替**:トグル ON/OFF で Premium 層の blur が外れるか（無料体験は変わらないこと）
5. **モバイル**:幅 375px(iPhone SE 相当)で崩れないか

将来は Vitest 等の単体テストを `src/__tests__/` 配下に置く想定。

---

## コミット規約(目安)

- `feat: 動物占いの60分類化` — 機能追加
- `fix: 九星気学の立春計算ずれ` — バグ修正
- `content: タロット解釈の表現改善` — 文言変更
- `style: モーダルの余白調整` — 見た目調整
- `refactor: calculations を ESM 化` — 内部構造変更
- `docs: README 更新` — ドキュメント

---

## 開発時のチェックリスト

新機能の PR を作る前に:

- [ ] 計算ロジックは純粋関数になっているか
- [ ] 辞書は content.js に集約したか
- [ ] 文言はトーンガイド(神秘的・押しつけがましくない)に従っているか
- [ ] モバイル幅で崩れていないか
- [ ] 既知の生年月日(上記)で結果が変わっていないか
- [ ] プレミアムロックを誤って外していないか
- [ ] `escapeHtml` を通して入力を描画しているか

---

## 連絡・引き継ぎメモ

- このコードベースは **プロトタイプ段階**。データの永続化、決済、認証は未実装。
- 設計の理由が不明な箇所は `git log` を辿るか、README の「将来の拡張ロードマップ」を確認。
- 占術の流派論争に巻き込まれそうな修正は、コメントに「○○流の式に従う」と明記する。
