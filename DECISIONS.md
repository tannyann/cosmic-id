# DECISIONS.md — 確定済みの意思決定

**ここに載っている決定は蒸し返さない。** 変更が必要な場合は、依頼主(Koyo)の再決定を経てからこのファイルを更新すること。
各項目には決定日と根拠(Issue/PR/コミット)を付す。実装が伴うものは反映済みかどうかを明記する。

---

## A. 占術の流派・方式(2026-07-11 決定 / 根拠: GitHub Issue #1 のコメント)

依頼主が5論点を一括決定。材料は `docs/audit/fortune-accuracy.md` と `docs/audit/parts/others.md`。

| # | 論点 | 決定 | 実装 |
|---|---|---|---|
| A1 | マヤ暦KINの起点 | 照合の結果「黒」(実装が誤り)だったため**公式ドリームスペル準拠に修正**(起点 2013-07-26 = KIN164、2/29はKINを持たない日としてスキップ) | 反映済み(PR #22) |
| A2 | タロット還元値22の扱い | **愚者(0)にマップ**(Mary K. Greer 方式) | 反映済み(PR #17) |
| A3 | lifePath の還元方式 | **個別還元方式**(年・月・日を個別に還元してから合計。Numerology.com/Decoz 系)。旧「全桁合計方式」から変更 | 反映済み(PR #21) |
| A4 | 東洋占術の年切替 | 干支・五行を九星と同じ**立春系に統一**+UIに注記 | 反映済み(PR #25) |
| A5 | 動物占いの精度 | **「独自12分類」と明記**する(公式60分類への互換化は見送り) | 反映済み(PR #24)。非互換の実証は PR #23 |

### A6. 立春の定義(2026-07-14 / 根拠: PR #20)

特定の暦注ではなく**天文計算**(太陽黄経315°の瞬間をJSTに換算した日付)を採用。
テーブルは `src/data/risshun.js`(1900–2100・自動生成。手編集禁止、再生成は `risshun.gen.mjs`)。

### A7. 計算結果の変動に対するユーザー告知(2026-07-11 / 根拠: Issue #1 注記)

**告知は不要**。理由: データ永続化・アカウントが未実装のプロトタイプ段階であり、
過去の診断結果が保存されていないため実害が限定的と判断した。

### A8. 2/29生まれのマヤ暦KIN(2026-07-15 / 根拠: `docs/learnings/maya-feb29-no-kin.md`)

公式ドリームスペルでは「KINを持たない日」だが、null を返すとUIが壊れるため
**2/28と同じKINを返す暫定仕様**とする。将来UIで注記する余地を残す。

---

## B. 開発体制・運用(2026-07-16 決定 / 根拠: `docs/ORG.md` v2)

### B1. 担当モデルはコスト階層順に検討する

**Cursor(定額) → Sonnet → Opus → Fable** の順に「これで足りないか」を上から検討する。
Opus は Sonnet が1回失敗したときの昇格先であり、先回りで使わない。**Fable は Issue設計と計算系PRのレビューのみで、実装・機械作業はしない。**

### B2. gitを文脈の保存先にする

分身(サブエージェント)は**着手時にブランチ → 意味のある単位ごとにcommit → こまめにpush**。
理由: 実運用で分身がセッション上限により多数中断し、**未コミットの成果が全損**した。git に載っていた成果だけが回収できた。

### B3. 並列は最大2体・委任プロンプトは10行以内

3体以上の同時起動はセッション上限の共倒れを招くため禁止。
文脈は git(Issue本文・docs/)に置き、プロンプトで再送しない。

### B4. 検証はピラミッド順

`npm test`(最安)→ node ワンライナー → ブラウザ実機(UI変更のPRのみ)。スクリーンショットは1PRにつき1枚まで。

### B5. レビューは作者以外・マージは人間

同一アカウントのPRはGitHub仕様で自己approveできないため、レビュー結果は**PRコメント**で記録する。
同一Issueで2回差し戻しが起きたら担当を1段上のモデルへ昇格する。

---

## C. 情報管理(2026-07-26 決定)

### C1. タスクの正本は GitHub Issues

`TASKS.md` 等のファイルは**作らない**(ファイルとIssueの二重管理は必ず腐るため)。
`docs/ISSUES.md` は設計書であり、進捗の正本ではない。

### C2. AIエージェントの入口は `AI_INSTRUCTIONS.md`

索引のみを置き、内容は各正本(`AGENTS.md` / `docs/ORG.md` / 本ファイル)に置く。重複を作らない。

### C3. Codexスキルの実体はリポジトリに置く(個人環境からはリンク)

Codex の実行時参照先は `~/.codex/skills/` だが、他AIから見えるよう**プロジェクト専用スキルのみ**
`.codex/skills/` に実体を置き、`~/.codex/skills/` からシンボリックリンクを張る(実体は1つ)。詳細は `AI_INSTRUCTIONS.md` §3。プロジェクト横断スキルはリポジトリに含めない。

### C4. テスト期待値には出どころを明記する

`[既知値]`(ドキュメント由来)/ `[外部照合]`(一般に知られる事実)/ `[回帰ピン]`(現状実装の固定=正しさの保証ではない)。
回帰ピンの不一致はピン側の更新を検討してよいが、既知値・外部照合の不一致は**実装バグとして報告**する(期待値を勝手に曲げない)。

---

## D. 人格観測システム（2026-08-12 決定 / 根拠: Koyo再決定・GitHub Issue #29）

旧検討案「表12タイプ × 裏12タイプ = 144通り」を採用せず、回答式の人格観測と
生年月日由来の象徴を分離する。詳細仕様は `docs/plan/persona-architecture.md`。

### D1. 中核IDは8 × 8 = 64通り

- **DAY SIGNAL（表）8タイプ**: 本人の回答から、外に現れやすい反応・行動を示す。
- **NIGHT GRAVITY（裏）8タイプ**: 本人の追加回答から、選択を内側から引く欲求・価値観を示す。
- 両者を組み合わせた64通りを、共有・比較・自己理解の中核IDとする。

実装状態: **未実装**。名称、質問、採点、結果文はパイロット検証後に確定する。

### D2. 表と裏はどちらも回答から導く

生年月日・名前・既存19体系の結果で、DAY SIGNALまたはNIGHT GRAVITYを決定・補正しない。
表を「作った仮面」、裏を「本当の自分」と上下づけず、異なる観測面として扱う。

### D3. 12 Cosmic Coordinatesは人格タイプではない

生年月日から得る12のCosmic Coordinatesを、64通りの中核IDへ添える**象徴的な補助座標**とする。
占術由来のLoreであり、心理検査・科学的な人格分類とは称さない。8 × 8 × 12を「768人格」として
販売・ランキング・希少性訴求に使わない。

### D4. 既存19体系はCOSMIC ATLASとして維持する

既存の計算結果を人格観測の採点材料にせず、ユーザーが任意で深く探索する別レイヤーに再配置する。
今回の決定だけでは、19体系の計算ロジック・無料範囲・Premium設定を変更しない。

### D5. 深層の基本結果は課金ロックしない

「課金しなければ本当の自分が分からない」という導線を禁止する。将来の課金対象は、保存、長文、
装丁、出力、継続記録などの追加価値として別途決定する。

### D6. 旧12 × 12資料の扱い

`docs/plan/persona-two-faces.md` は調査・比較の履歴として残すが、**旧案であり実装根拠にしない**。
DAY SIGNAL / NIGHT GRAVITY / PARALLAX、各タイプ名、座標の公開名称は現時点では作業名称であり、
構造の決定と名称の最終決定を分ける。

---

## E. 保留中(まだ決まっていない = ここに書けないもの)

以下は**未決定**であり、AIが勝手に決めてはならない。詳細は `docs/プロジェクト引き継ぎ書.md` §5。

- Premium の**価格**・**解放範囲**・**課金アーキテクチャ**(Payment Link継続か、サーバー検証へ移行か)
- Premiumゲートの再有効化タイミング(現在 `PREMIUM_CONFIG.enabled = false` で全章無料)
- マヤ暦ガイドKINの紋章オフセット表の一次資料照合(実装は定説準拠で動作中)
- 月星座の外部サイト照合(テストは現在 astronomy-engine 出力の回帰ピン)
